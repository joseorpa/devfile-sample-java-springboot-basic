# Enable graceful shutdown (available in Spring Boot 2.3+)
server.shutdown=graceful

# Configure graceful shutdown timeout (default is 30s)
spring.lifecycle.timeout-per-shutdown-phase=20s

# Optional: Configure server port for easier testing
server.port=8081
